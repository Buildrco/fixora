import { Ionicons } from "@expo/vector-icons";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { useState } from "react";
import { colors, radius } from "../../constants/theme";
import { DetailSection, ProfileDetailShell } from "../../components/ProfileDetailShell";
const faqs = [{ q: "How do I request a repair?", a: "Choose an issue, select your device series, then add the details a repairer should know." }, { q: "How are repairers verified?", a: "Fixora profiles highlight experience, ratings, and the services each repairer offers." }, { q: "Can I change my device later?", a: "Yes. Tap the device row in Request a repair to choose a different category, brand, or series." }];
export default function Help() { const [open, setOpen] = useState(0); return <ProfileDetailShell title="Help & support" eyebrow="WE’RE HERE TO HELP" subtitle="Quick answers for the moments when a little guidance saves a lot of time."><View style={s.contact}><View style={s.contactIcon}><Text style={s.contactEmoji}>💬</Text></View><View style={{ flex: 1 }}><Text style={s.contactTitle}>Talk to the Fixora team</Text><Text style={s.contactCopy}>Usually replies within a few hours.</Text></View><Ionicons name="arrow-forward" size={18} color={colors.ink} /></View><DetailSection title="Frequently asked"><View style={s.faq}>{faqs.map((faq, index) => <Pressable key={faq.q} onPress={() => setOpen(open === index ? -1 : index)} style={s.faqItem}><View style={s.faqHead}><Text style={s.question}>{faq.q}</Text><Ionicons name={open === index ? "remove" : "add"} size={18} color={colors.muted} /></View>{open === index && <Text style={s.answer}>{faq.a}</Text>}</Pressable>)}</View></DetailSection><DetailSection title="More ways to reach us"><View style={s.link}><Ionicons name="mail-outline" size={20} color={colors.blue} /><Text style={s.linkText}>Email support</Text><Ionicons name="chevron-forward" size={17} color={colors.muted} /></View><View style={s.link}><Ionicons name="book-outline" size={20} color={colors.blue} /><Text style={s.linkText}>Read the repair guide</Text><Ionicons name="chevron-forward" size={17} color={colors.muted} /></View></DetailSection></ProfileDetailShell>; }
const s = StyleSheet.create({ contact: { padding: 15, borderRadius: radius.lg, backgroundColor: colors.accentSoft, flexDirection: "row", alignItems: "center", gap: 11 }, contactIcon: { width: 42, height: 42, borderRadius: 14, backgroundColor: "#fff", alignItems: "center", justifyContent: "center" }, contactEmoji: { fontSize: 22 }, contactTitle: { color: colors.ink, fontSize: 14, fontWeight: "800" }, contactCopy: { color: colors.muted, fontSize: 11, marginTop: 3 }, faq: { borderTopWidth: 1, borderTopColor: colors.line }, faqItem: { paddingVertical: 16, borderBottomWidth: 1, borderBottomColor: colors.line }, faqHead: { flexDirection: "row", justifyContent: "space-between", gap: 10 }, question: { color: colors.ink, fontSize: 13, fontWeight: "800", flex: 1 }, answer: { color: colors.muted, fontSize: 12, lineHeight: 19, marginTop: 9 }, link: { minHeight: 54, borderBottomWidth: 1, borderBottomColor: colors.line, flexDirection: "row", alignItems: "center", gap: 11 }, linkText: { color: colors.ink, fontSize: 13, fontWeight: "700", flex: 1 } });