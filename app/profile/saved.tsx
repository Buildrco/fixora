import { Ionicons } from "@expo/vector-icons";
import { Image, Pressable, StyleSheet, Text, View } from "react-native";
import { useRouter } from "expo-router";
import { colors, radius } from "../../constants/theme";
import { DetailSection, ProfileDetailShell } from "../../components/ProfileDetailShell";
const saved = [{ title: "How to find a short on the power rail", by: "Kwame Repairs", kind: "Tutorial", image: "https://images.unsplash.com/photo-1581092160562-40aa08e78837?auto=format&fit=crop&w=800&q=85" }, { title: "iPhone 13 screen replacement", by: "Ama’s Phone Clinic", kind: "Repair story", image: "https://images.unsplash.com/photo-1601784551446-20c9e07cdbdb?auto=format&fit=crop&w=800&q=85" }, { title: "Five tools every beginner needs", by: "Fixora Learn", kind: "Guide", image: "https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=800&q=85" }];
export default function Saved() { const router = useRouter(); return <ProfileDetailShell title="Saved posts" eyebrow="YOUR COLLECTION" subtitle="The tutorials, repair stories, and useful finds you wanted to keep."><DetailSection title="Saved for later" action="12 saved">{saved.map(item => <Pressable key={item.title} onPress={() => router.push("/community" as never)} style={({ pressed }) => [s.item, pressed && s.pressed]}><Image source={{ uri: item.image }} style={s.image} /><View style={s.copy}><View style={s.kind}><Text style={s.kindText}>{item.kind}</Text><Ionicons name="bookmark" size={15} color={colors.accent} /></View><Text style={s.title}>{item.title}</Text><Text style={s.by}>{item.by}</Text></View><Ionicons name="chevron-forward" size={18} color={colors.muted} /></Pressable>)}</DetailSection></ProfileDetailShell>; }
const s = StyleSheet.create({ item: { minHeight: 92, padding: 10, borderWidth: 1, borderColor: colors.line, borderRadius: radius.lg, flexDirection: "row", alignItems: "center", gap: 11, marginBottom: 10 }, image: { width: 72, height: 72, borderRadius: 15 }, copy: { flex: 1 }, kind: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, kindText: { color: colors.accent, fontSize: 10, fontWeight: "800", textTransform: "uppercase", letterSpacing: 0.6 }, title: { color: colors.ink, fontSize: 13, lineHeight: 18, fontWeight: "800", marginTop: 5 }, by: { color: colors.muted, fontSize: 11, marginTop: 4 }, pressed: { opacity: 0.78, transform: [{ scale: 0.985 }] } });