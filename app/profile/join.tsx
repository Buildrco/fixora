import { Ionicons } from "@expo/vector-icons";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { useState } from "react";
import { colors, radius } from "../../constants/theme";
import { ProfileDetailShell } from "../../components/ProfileDetailShell";
const roles = [{ key: "repairer", emoji: "🛠️", title: "I repair devices", copy: "Get matched with people who need your skills." }, { key: "vendor", emoji: "📦", title: "I sell tech", copy: "List products and reach people who are ready to buy." }];
export default function Join() { const [role, setRole] = useState("repairer"); return <ProfileDetailShell title="Join Fixora" eyebrow="GROW WITH THE COMMUNITY" subtitle="Turn what you know into useful work, trusted recommendations, and new customers."><View style={s.hero}><Text style={s.heroEmoji}>🌱</Text><Text style={s.heroTitle}>Your next opportunity can start here.</Text><Text style={s.heroCopy}>Choose how you want to show up on Fixora.</Text></View><Text style={s.section}>I want to...</Text>{roles.map(item => <Pressable key={item.key} onPress={() => setRole(item.key)} style={[s.role, role === item.key && s.roleActive]}><Text style={s.emoji}>{item.emoji}</Text><View style={s.copy}><Text style={s.roleTitle}>{item.title}</Text><Text style={s.roleCopy}>{item.copy}</Text></View><View style={[s.radio, role === item.key && s.radioActive]}>{role === item.key && <View style={s.dot} />}</View></Pressable>)}<Pressable onPress={() => setRole(role)} style={s.cta}><Text style={s.ctaText}>Continue as a {role}</Text><Ionicons name="arrow-forward" size={18} color="#fff" /></Pressable></ProfileDetailShell>; }
const s = StyleSheet.create({ hero: { padding: 18, borderRadius: radius.xl, backgroundColor: colors.accentSoft, marginTop: 20 }, heroEmoji: { fontSize: 31 }, heroTitle: { color: colors.ink, fontSize: 18, lineHeight: 23, fontWeight: "800", marginTop: 14 }, heroCopy: { color: colors.muted, fontSize: 12, marginTop: 5 }, section: { color: colors.ink, fontSize: 15, fontWeight: "800", marginTop: 25, marginBottom: 11 }, role: { minHeight: 86, borderWidth: 1, borderColor: colors.line, borderRadius: radius.lg, padding: 13, flexDirection: "row", alignItems: "center", gap: 11, marginBottom: 10 }, roleActive: { borderColor: colors.blue, backgroundColor: "rgba(45,127,249,.07)" }, emoji: { fontSize: 28 }, copy: { flex: 1 }, roleTitle: { color: colors.ink, fontSize: 14, fontWeight: "800" }, roleCopy: { color: colors.muted, fontSize: 11, lineHeight: 16, marginTop: 4 }, radio: { width: 21, height: 21, borderRadius: 11, borderWidth: 1.5, borderColor: colors.line, alignItems: "center", justifyContent: "center" }, radioActive: { borderColor: colors.blue }, dot: { width: 10, height: 10, borderRadius: 5, backgroundColor: colors.blue }, cta: { height: 54, borderRadius: 18, backgroundColor: colors.ink, alignItems: "center", justifyContent: "center", flexDirection: "row", gap: 9, marginTop: 14 }, ctaText: { color: "#fff", fontSize: 15, fontWeight: "800" } });