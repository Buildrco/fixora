import { Ionicons } from "@expo/vector-icons";
import { useRouter } from "expo-router";
import { Image, Pressable, StyleSheet, Text, View } from "react-native";
import { colors, radius } from "../../constants/theme";
import { DetailSection, ProfileDetailShell, detailStyles } from "../../components/ProfileDetailShell";

const orders = [{ name: "iPhone 13 Pro", issue: "Broken screen", status: "Repair in progress", date: "Today · 10:42 AM", image: "https://images.unsplash.com/photo-1601784551446-20c9e07cdbdb?auto=format&fit=crop&w=700&q=85" }, { name: "MacBook Air M2", issue: "Battery check", status: "Completed", date: "Aug 24 · 2:16 PM", image: "https://images.unsplash.com/photo-1517336714739-489689fd1ca8?auto=format&fit=crop&w=700&q=85" }];
export default function Orders() { const router = useRouter(); return <ProfileDetailShell title="Orders" eyebrow="YOUR REPAIR HISTORY" subtitle="Keep track of repairs, estimates, and completed work in one place."><DetailSection title="Recent repairs" action="2 total">{orders.map(order => <View key={order.name} style={s.card}><Image source={{ uri: order.image }} style={s.image} /><View style={s.copy}><View style={s.row}><Text style={s.name}>{order.name}</Text><Ionicons name="ellipsis-horizontal" size={17} color={colors.muted} /></View><Text style={s.issue}>{order.issue}</Text><Text style={s.date}>{order.date}</Text><View style={s.status}><View style={[s.dot, order.status === "Completed" && s.done]} /><Text style={s.statusText}>{order.status}</Text></View><Pressable onPress={() => router.push("/repair/confirmation" as never)} style={s.button}><Text style={s.buttonText}>{order.status === "Completed" ? "View details" : "Track repair"}</Text><Ionicons name="arrow-forward" size={15} color={colors.blue} /></Pressable></View></View>)}</DetailSection><View style={detailStyles.empty}><Text style={detailStyles.emptyEmoji}>🧾</Text><Text style={detailStyles.emptyTitle}>Every repair, kept close</Text><Text style={detailStyles.emptyCopy}>Your next request will appear here automatically.</Text></View></ProfileDetailShell>; }
const s = StyleSheet.create({ card: { borderWidth: 1, borderColor: colors.line, borderRadius: radius.lg, overflow: "hidden", marginBottom: 12, backgroundColor: colors.card }, image: { width: "100%", height: 130 }, copy: { padding: 13 }, row: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, name: { color: colors.ink, fontSize: 15, fontWeight: "800" }, issue: { color: colors.muted, fontSize: 13, marginTop: 4 }, date: { color: colors.muted, fontSize: 11, marginTop: 9 }, status: { flexDirection: "row", alignItems: "center", gap: 6, marginTop: 11 }, dot: { width: 7, height: 7, borderRadius: 4, backgroundColor: colors.accent }, done: { backgroundColor: colors.green }, statusText: { color: colors.ink, fontSize: 12, fontWeight: "700" }, button: { flexDirection: "row", alignItems: "center", gap: 6, marginTop: 13 }, buttonText: { color: colors.blue, fontSize: 12, fontWeight: "800" } });