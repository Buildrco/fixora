import { Ionicons } from "@expo/vector-icons";
import { Pressable, ScrollView, StyleSheet, Text, View, Image } from "react-native";
import { useRouter } from "expo-router";
import { colors, radius } from "../../constants/theme";
import { DetailSection, ProfileDetailShell } from "../../components/ProfileDetailShell";
const courses = [{ title: "Microsoldering foundations", meta: "Kwame Repairs · 8 lessons", progress: "68%", image: "https://images.unsplash.com/photo-1581092160562-40aa08e78837?auto=format&fit=crop&w=800&q=85" }, { title: "Diagnose a charging port", meta: "Fixora Learn · 5 lessons", progress: "32%", image: "https://images.unsplash.com/photo-1583863788434-e58a36330cf0?auto=format&fit=crop&w=800&q=85" }];
export default function Learning() { const router = useRouter(); return <ProfileDetailShell title="My learning" eyebrow="KEEP BUILDING" subtitle="Pick up where you left off and turn curiosity into repair confidence."><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.featureRow}><View style={s.feature}><Text style={s.featureEmoji}>🧠</Text><Text style={s.featureTitle}>Learn one useful thing today.</Text><Text style={s.featureCopy}>Short, practical lessons from people who repair every day.</Text></View><View style={[s.feature, s.featureBlue]}><Text style={s.featureEmoji}>⚡</Text><Text style={s.featureTitle}>3 day streak</Text><Text style={s.featureCopy}>You’re building a habit. Keep it going.</Text></View></ScrollView><DetailSection title="Continue learning" action="See all">{courses.map(course => <Pressable key={course.title} onPress={() => router.push("/learn" as never)} style={({ pressed }) => [s.course, pressed && s.pressed]}><Image source={{ uri: course.image }} style={s.thumb} /><View style={s.courseCopy}><Text style={s.courseTitle}>{course.title}</Text><Text style={s.meta}>{course.meta}</Text><View style={s.progressTrack}><View style={[s.progress, { width: course.progress }]} /></View><Text style={s.progressText}>{course.progress} complete</Text></View><Ionicons name="play-circle-outline" size={22} color={colors.blue} /></Pressable>)}</DetailSection></ProfileDetailShell>; }
const s = StyleSheet.create({ featureRow: { gap: 10, paddingTop: 20 }, feature: { width: 245, minHeight: 150, padding: 16, borderRadius: radius.xl, backgroundColor: colors.accentSoft }, featureBlue: { backgroundColor: "#EEF4FF" }, featureEmoji: { fontSize: 28 }, featureTitle: { color: colors.ink, fontSize: 16, fontWeight: "800", marginTop: 14 }, featureCopy: { color: colors.muted, fontSize: 12, lineHeight: 17, marginTop: 4 }, course: { minHeight: 94, padding: 10, borderWidth: 1, borderColor: colors.line, borderRadius: radius.lg, flexDirection: "row", alignItems: "center", gap: 11, marginBottom: 10 }, thumb: { width: 72, height: 72, borderRadius: 15 }, courseCopy: { flex: 1 }, courseTitle: { color: colors.ink, fontSize: 13, fontWeight: "800" }, meta: { color: colors.muted, fontSize: 11, marginTop: 4 }, progressTrack: { height: 5, borderRadius: 3, backgroundColor: colors.soft, marginTop: 10, overflow: "hidden" }, progress: { height: "100%", borderRadius: 3, backgroundColor: colors.blue }, progressText: { color: colors.muted, fontSize: 10, marginTop: 5 }, pressed: { opacity: 0.78, transform: [{ scale: 0.985 }] } });